# Hydra config for R-GCN (primary model).
# Params: hidden_dim=128, num_layers=2, dropout=0.2, epochs, lr, batch_size.
# Contrastive loss weight, relation-specific weight matrices, output collection name.
