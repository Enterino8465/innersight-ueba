# Evaluation Report

Documents the final model comparison across all five CERT scenarios.
Populated by running `worker evaluate` on each model and comparing the JSON outputs
in ml/experiments/. Must show R-GCN > GraphSAGE > Node2Vec > IsolationForest.
Required for v1.0 definition of done (blueprint §17).
