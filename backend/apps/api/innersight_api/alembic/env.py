# Alembic migration environment.
# Configures async engine from DATABASE_URL, imports Base.metadata so
# --autogenerate can diff ORM models against the live schema.
# Migration files live alongside this file as versions/XXXX_*.py.
